# P0 implementation readiness

This marker is created after the approved P0 implementation workflow was started. It is intentionally small and will be removed before merge if the implementation workflow has already completed.
